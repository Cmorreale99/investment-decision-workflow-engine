"""AI reasoning layer: role-specific agents.

Implemented in Phase 8 behind a ``ReasoningProvider`` interface. Must not
touch market data or execute trades. Phase 1 is intentionally empty so the
deterministic baseline runs with zero external dependencies.
"""
